{
  "name": "imigraeuropa",
  "version": "0.1.0",
  "private": true,
  "description": "Gerador estático sem dependências para o portal ImigraEuropa",
  "scripts": {
    "build": "node scripts/build.js --mode preview",
    "build:public": "node scripts/build.js --mode public",
    "build:pages": "node scripts/build.js --mode public --base /labellabell/",
    "serve": "node scripts/serve.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}

